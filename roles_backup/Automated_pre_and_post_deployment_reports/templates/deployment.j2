<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FMC Deployment Report - {{ report_data.execution_id }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        
        .container {
            max-width: 1400px;
            margin: 0 auto;
            background: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            overflow: hidden;
        }
        
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 40px;
            text-align: center;
        }
        
        .header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
        }
        
        .header .subtitle {
            font-size: 1.2em;
            opacity: 0.9;
        }
        
        .report-meta {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-top: 20px;
            flex-wrap: wrap;
        }
        
        .meta-item {
            background: rgba(255, 255, 255, 0.2);
            padding: 10px 20px;
            border-radius: 10px;
            backdrop-filter: blur(10px);
        }
        
        .content {
            padding: 40px;
        }
        
        .summary-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }
        
        .summary-card {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }
        
        .summary-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
        }
        
        .summary-card h3 {
            color: #4a5568;
            margin-bottom: 15px;
            font-size: 1.1em;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .summary-card .value {
            font-size: 2.5em;
            font-weight: bold;
            color: #2d3748;
            margin-bottom: 5px;
        }
        
        .summary-card .label {
            color: #718096;
            font-size: 0.9em;
        }
        
        .section {
            margin-bottom: 40px;
        }
        
        .section-title {
            font-size: 1.8em;
            color: #2d3748;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 3px solid #667eea;
        }
        
        .comparison-container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin-bottom: 30px;
        }
        
        .comparison-box {
            background: #f8f9fa;
            padding: 25px;
            border-radius: 15px;
            border-left: 5px solid #667eea;
        }
        
        .comparison-box.post {
            border-left-color: #48bb78;
        }
        
        .comparison-box h4 {
            color: #4a5568;
            margin-bottom: 15px;
            font-size: 1.2em;
        }
        
        .stat-row {
            display: flex;
            justify-content: space-between;
            padding: 10px 0;
            border-bottom: 1px solid #e2e8f0;
        }
        
        .stat-row:last-child {
            border-bottom: none;
        }
        
        .stat-label {
            color: #718096;
        }
        
        .stat-value {
            font-weight: bold;
            color: #2d3748;
        }
        
        .devices-table {
            width: 100%;
            background: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }
        
        .devices-table thead {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }
        
        .devices-table th {
            padding: 15px;
            text-align: left;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.85em;
            letter-spacing: 1px;
        }
        
        .devices-table td {
            padding: 15px;
            border-bottom: 1px solid #e2e8f0;
        }
        
        .devices-table tbody tr:hover {
            background: #f7fafc;
        }
        
        .devices-table tbody tr:last-child td {
            border-bottom: none;
        }
        
        .badge {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.85em;
            font-weight: 600;
        }
        
        .badge-success {
            background: #c6f6d5;
            color: #22543d;
        }
        
        .badge-warning {
            background: #fed7aa;
            color: #7c2d12;
        }
        
        .badge-danger {
            background: #fed7d7;
            color: #742a2a;
        }
        
        .badge-info {
            background: #bee3f8;
            color: #2c5282;
        }
        
        .progress-bar {
            width: 100%;
            height: 8px;
            background: #e2e8f0;
            border-radius: 4px;
            overflow: hidden;
            margin-top: 5px;
        }
        
        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
            border-radius: 4px;
            transition: width 0.3s ease;
        }
        
        .deployment-status {
            background: #f7fafc;
            padding: 30px;
            border-radius: 15px;
            margin-top: 30px;
        }
        
        .status-icon {
            font-size: 3em;
            margin-bottom: 15px;
            text-align: center;
        }
        
        .status-success {
            color: #48bb78;
        }
        
        .status-warning {
            color: #ed8936;
        }
        
        .status-info {
            color: #4299e1;
        }
        
        .footer {
            background: #2d3748;
            color: white;
            padding: 30px;
            text-align: center;
        }
        
        .footer-info {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-top: 15px;
            flex-wrap: wrap;
        }
        
        .footer-item {
            opacity: 0.8;
        }
        
        @media (max-width: 768px) {
            .comparison-container {
                grid-template-columns: 1fr;
            }
            
            .summary-grid {
                grid-template-columns: 1fr;
            }
            
            .devices-table {
                font-size: 0.9em;
            }
            
            .devices-table th,
            .devices-table td {
                padding: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>🔥 FMC Deployment Report</h1>
            <div class="subtitle">Comprehensive Pre/Post Deployment Analysis</div>
            <div class="report-meta">
                <div class="meta-item">
                    <strong>Report ID:</strong> {{ report_data.execution_id }}
                </div>
                <div class="meta-item">
                    <strong>Generated:</strong> {{ report_data.report_date }} {{ report_data.report_time }}
                </div>
                <div class="meta-item">
                    <strong>Type:</strong> {{ report_data.deployment_type }}
                </div>
            </div>
        </div>
        
        <!-- Content -->
        <div class="content">
            <!-- Summary Cards -->
            <div class="summary-grid">
                <div class="summary-card">
                    <h3>Total Devices</h3>
                    <div class="value">{{ report_data.deployment_analysis.total_devices }}</div>
                    <div class="label">Managed Devices</div>
                </div>
                
                <div class="summary-card">
                    <h3>Pending Changes</h3>
                    <div class="value">{{ report_data.deployment_analysis.total_pending_changes }}</div>
                    <div class="label">Before Deployment</div>
                </div>
                
                <div class="summary-card">
                    <h3>Devices with Changes</h3>
                    <div class="value">{{ report_data.deployment_analysis.devices_with_changes | length }}</div>
                    <div class="label">Require Deployment</div>
                </div>
                
                <div class="summary-card">
                    <h3>Deployment Status</h3>
                    <div class="value">
                        {% if report_data.deployment_executed %}
                            <span style="color: #48bb78;">✓</span>
                        {% else %}
                            <span style="color: #4299e1;">ℹ</span>
                        {% endif %}
                    </div>
                    <div class="label">
                        {% if report_data.deployment_executed %}
                            Executed
                        {% else %}
                            Report Only
                        {% endif %}
                    </div>
                </div>
            </div>
            
            <!-- Pre/Post Comparison -->
            <div class="section">
                <h2 class="section-title">📊 Deployment Comparison</h2>
                <div class="comparison-container">
                    <div class="comparison-box">
                        <h4>Pre-Deployment Status</h4>
                        <div class="stat-row">
                            <span class="stat-label">Timestamp:</span>
                            <span class="stat-value">{{ report_data.pre_deployment_timestamp | default('N/A') }}</span>
                        </div>
                        <div class="stat-row">
                            <span class="stat-label">Total Devices:</span>
                            <span class="stat-value">{{ report_data.deployment_analysis.total_devices }}</span>
                        </div>
                        <div class="stat-row">
                            <span class="stat-label">Devices with Changes:</span>
                            <span class="stat-value">{{ report_data.deployment_analysis.devices_with_changes | length }}</span>
                        </div>
                        <div class="stat-row">
                            <span class="stat-label">Total Pending Changes:</span>
                            <span class="stat-value">{{ report_data.deployment_analysis.total_pending_changes }}</span>
                        </div>
                    </div>
                    
                    <div class="comparison-box post">
                        <h4>Post-Deployment Status</h4>
                        <div class="stat-row">
                            <span class="stat-label">Timestamp:</span>
                            <span class="stat-value">{{ report_data.post_deployment_timestamp | default('N/A') }}</span>
                        </div>
                        <div class="stat-row">
                            <span class="stat-label">Total Devices:</span>
                            <span class="stat-value">{{ report_data.post_deployment.items | length }}</span>
                        </div>
                        <div class="stat-row">
                            <span class="stat-label">Devices with Changes:</span>
                            <span class="stat-value">{{ report_data.post_deployment.items | selectattr('pendingChanges', 'gt', 0) | list | length }}</span>
                        </div>
                        <div class="stat-row">
                            <span class="stat-label">Remaining Changes:</span>
                            <span class="stat-value">{{ report_data.post_deployment.items | map(attribute='pendingChanges') | select('number') | sum }}</span>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Device Details Table -->
            <div class="section">
                <h2 class="section-title">🖥️ Device Details</h2>
                <table class="devices-table">
                    <thead>
                        <tr>
                            <th>Device Name</th>
                            <th>Type</th>
                            <th>Model</th>
                            <th>Version</th>
                            <th>Pre-Deploy Changes</th>
                            <th>Post-Deploy Changes</th>
                            <th>Health Status</th>
                            <th>Access Policy</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for device in report_data.detailed_devices %}
                        <tr>
                            <td><strong>{{ device.device_name }}</strong></td>
                            <td>{{ device.device_type }}</td>
                            <td>{{ device.model | default('N/A') }}</td>
                            <td>{{ device.software_version | default('N/A') }}</td>
                            <td>
                                {% if device.pending_changes > 0 %}
                                    <span class="badge badge-warning">{{ device.pending_changes }}</span>
                                {% else %}
                                    <span class="badge badge-success">0</span>
                                {% endif %}
                            </td>
                            <td>
                                {% set post_changes = 0 %}
                                {% for post_device in report_data.post_deployment.items %}
                                    {% if post_device.device.id == device.device_id %}
                                        {% set post_changes = post_device.pendingChanges %}
                                    {% endif %}
                                {% endfor %}
                                {% if post_changes > 0 %}
                                    <span class="badge badge-warning">{{ post_changes }}</span>
                                {% else %}
                                    <span class="badge badge-success">0</span>
                                {% endif %}
                            </td>
                            <td>
                                {% if device.health_status == 'green' or device.health_status == 'healthy' %}
                                    <span class="badge badge-success">{{ device.health_status | upper }}</span>
                                {% elif device.health_status == 'yellow' or device.health_status == 'warning' %}
                                    <span class="badge badge-warning">{{ device.health_status | upper }}</span>
                                {% elif device.health_status == 'red' or device.health_status == 'critical' %}
                                    <span class="badge badge-danger">{{ device.health_status | upper }}</span>
                                {% else %}
                                    <span class="badge badge-info">{{ device.health_status | default('UNKNOWN') | upper }}</span>
                                {% endif %}
                            </td>
                            <td>{{ device.access_policy | default('N/A') }}</td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
            
            <!-- Deployment Status -->
            {% if report_data.deployment_executed %}
            <div class="section">
                <h2 class="section-title">🚀 Deployment Execution Results</h2>
                <div class="deployment-status">
                    <div class="status-icon status-success">✓</div>
                    <h3 style="text-align: center; margin-bottom: 20px;">Deployment Executed Successfully</h3>
                    
                    {% if report_data.deployment_results | length > 0 %}
                    <div style="margin-top: 20px;">
                        <h4 style="margin-bottom: 15px;">Deployment Tasks:</h4>
                        {% for result in report_data.deployment_results %}
                        <div class="stat-row">
                            <span class="stat-label">Task {{ loop.index }}:</span>
                            <span class="stat-value">
                                {{ result.json.status | default('PENDING') }}
                                {% if result.json.metadata is defined and result.json.metadata.task is defined %}
                                (ID: {{ result.json.metadata.task.id }})
                                {% endif %}
                            </span>
                        </div>
                        {% endfor %}
                    </div>
                    {% endif %}
                </div>
            </div>
            {% else %}
            <div class="section">
                <h2 class="section-title">ℹ️ Deployment Status</h2>
                <div class="deployment-status">
                    <div class="status-icon status-info">ℹ</div>
                    <h3 style="text-align: center; margin-bottom: 20px;">Report Only Mode</h3>
                    <p style="text-align: center; color: #718096;">
                        This report was generated in report-only mode. No actual deployment was executed.
                        {% if report_data.deployment_analysis.deployment_required %}
                        <br><br>
                        <strong>{{ report_data.deployment_analysis.devices_with_changes | length }}</strong> device(s) have pending changes that require deployment.
                        {% else %}
                        <br><br>
                        No devices currently require deployment.
                        {% endif %}
                    </p>
                </div>
            </div>
            {% endif %}
            
            <!-- Summary Statistics -->
            <div class="section">
                <h2 class="section-title">📈 Summary Statistics</h2>
                <div class="summary-grid">
                    <div class="summary-card">
                        <h3>Deployment Efficiency</h3>
                        {% set efficiency = 0 %}
                        {% if report_data.deployment_analysis.total_pending_changes > 0 %}
                            {% set post_changes = report_data.post_deployment.items | map(attribute='pendingChanges') | select('number') | sum %}
                            {% set efficiency = ((report_data.deployment_analysis.total_pending_changes - post_changes) / report_data.deployment_analysis.total_pending_changes * 100) | round(1) %}
                        {% endif %}
                        <div class="value">{{ efficiency }}%</div>
                        <div class="progress-bar">
                            <div class="progress-fill" style="width: {{ efficiency }}%"></div>
                        </div>
                        <div class="label">Changes Deployed</div>
                    </div>
                    
                    <div class="summary-card">
                        <h3>Device Coverage</h3>
                        {% set coverage = 0 %}
                        {% if report_data.deployment_analysis.total_devices > 0 %}
                            {% set coverage = (report_data.deployment_analysis.devices_with_changes | length / report_data.deployment_analysis.total_devices * 100) | round(1) %}
                        {% endif %}
                        <div class="value">{{ coverage }}%</div>
                        <div class="progress-bar">
                            <div class="progress-fill" style="width: {{ coverage }}%"></div>
                        </div>
                        <div class="label">Devices Affected</div>
                    </div>
                    
                    <div class="summary-card">
                        <h3>Average Changes</h3>
                        {% set avg_changes = 0 %}
                        {% if report_data.deployment_analysis.devices_with_changes | length > 0 %}
                            {% set avg_changes = (report_data.deployment_analysis.total_pending_changes / report_data.deployment_analysis.devices_with_changes | length) | round(1) %}
                        {% endif %}
                        <div class="value">{{ avg_changes }}</div>
                        <div class="label">Per Device</div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <div style="font-size: 1.2em; margin-bottom: 10px;">
                Generated by Ansible Automation Platform
            </div>
            <div class="footer-info">
                <div class="footer-item">
                    <strong>FMC Host:</strong> {{ ansible_host }}
                </div>
                <div class="footer-item">
                    <strong>Generated By:</strong> {{ ansible_user }}
                </div>
                <div class="footer-item">
                    <strong>Report Type:</strong> {{ report_data.deployment_type }}
                </div>
            </div>
        </div>
    </div>
</body>
</html>